/**
 * Environment variable for the local environment
 * Basically no key are set to avoid any pollution
 */

var SN = window.SN = window.SN || {};

SN.env = {
    /**
     * Node URL
     */
    nodeBaseUrl: 'http://localhost:3000'
};
